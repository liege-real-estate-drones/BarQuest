'use client';
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

export function TalentsView() {
    return (
        <Card>
            <CardHeader>
                <CardTitle>Talents</CardTitle>
            </CardHeader>
            <CardContent>
                <p>Talent tree will be displayed here.</p>
            </CardContent>
        </Card>
    );
}
